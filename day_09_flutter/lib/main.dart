import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
      title: 'Title',
      home: Scaffold(
        appBar: AppBar(
          title: const Text('Text'),
          backgroundColor: Colors.green[98],
        ),
      )));
}
